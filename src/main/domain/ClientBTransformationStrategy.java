public class ClientBTransformationStrategy implements TransformationStrategy{
    @Override
    public void transform(String inputXmlPath, String outputXmlPath) {
        // Transformation logic for Client B - empty since we don't have it
    }
}
