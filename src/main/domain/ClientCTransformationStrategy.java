public class ClientCTransformationStrategy implements TransformationStrategy {
    @Override
    public void transform(String inputXmlPath, String outputXmlPath) {
        // Transformation logic for Client C - also empty like for client B since we also don't have this one
    }
}
